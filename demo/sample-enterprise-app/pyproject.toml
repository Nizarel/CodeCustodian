[project]
name = "enterprise-inventory-app"
version = "2.3.1"
description = "Enterprise inventory management system — legacy codebase"
requires-python = ">=3.11"

dependencies = [
    "pandas>=1.5",
    "numpy>=1.24",
    "flask>=2.3",
    "requests>=2.28",
]
